matrix:
  include:
  - dist: xenial
    language: minimal
    os: linux
    services:
    - docker
    ###- os: osx    # TODO(bassosimone): we need pre-compiled MK
script:
- ./script/download-mmdb.sh
- ./script/build-travis-${TRAVIS_OS_NAME}.sh
